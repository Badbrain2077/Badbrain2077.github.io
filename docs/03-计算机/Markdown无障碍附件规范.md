无奈之举，obsidian没有使用该文件名的功能，Vscode、Typora用文件名做文件夹名，改名字就不太方便。  

只能说有利有弊，没有完美的解决方案。  

而且重要的是做笔记，不是捣鼓这些。

---

每个Markdown编辑器的附件添加方式都不太一样，尽量统一，方便转移。

文件名统一：同级根目录 `assets/xxx.jpg`

---

Obsidian：注意关闭Wiki格式，切换相对路径，更改附件储存地址文件夹名字`assets`

Typora：复制到指定路径，`./assets`

vscode：`Office Viewer` 设置更改储存方式 `assets/${now}`